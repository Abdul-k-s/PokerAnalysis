<Window x:Class="PokerAnalysis.Views.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokerAnalysis.Views"
             mc:Ignorable="d" 

             d:DesignHeight="450" d:DesignWidth="800" Background="LightGray">
    <Grid>

        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ListView  ItemsSource="{Binding PlayerStr}" Grid.Row="0" Grid.Column="0" Margin="5 5 0 0">

                <ListView.View>
                    <GridView >
                        
                        <GridViewColumn Header="Player" DisplayMemberBinding="{Binding Label}" />
                        <GridViewColumn Header="Wins" DisplayMemberBinding="{Binding Win}"/>
                        <GridViewColumn Header="Win%" DisplayMemberBinding="{Binding WinsPercentage}"/>

                    </GridView>
                </ListView.View>
            </ListView>

            <ListView ItemsSource="{Binding HandStr}" Grid.Row="1" Grid.Column="0" Margin="5 0 0 5">
                <ListView.View>
                    <GridView>

                        <GridViewColumn Header="HandType" DisplayMemberBinding="{Binding Item1}"/>
                        <GridViewColumn Header="Count" DisplayMemberBinding="{Binding Item2}"/>
                        <GridViewColumn Header="Percentage" DisplayMemberBinding="{Binding Item3}"/>

                    </GridView>
                </ListView.View>
            </ListView>

        </Grid>
    

    </Grid>
</Window>
